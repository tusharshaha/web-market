/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['htmldemo.net'], // Add the domain of the image here
  },
};

module.exports = nextConfig;
